import Material 0.2
import QtQuick 2.2

Rectangle {

    anchors {
        top: parent.top;
        left: parent.left;
        right: parent.right;
        margins: 0
        bottomMargin: 0
    }

    height: Units.dp(50)
    color:  "transparent"
}
